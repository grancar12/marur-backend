server.port=8081

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/marur}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Sena2020}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.application.name=marur-backend

jwt.secret=${JWT_SECRET:marur2024secretkey_must_be_at_least_32_characters_long}
jwt.expiration=${JWT_EXPIRATION:86400000}

wompi.public-key=${WOMPI_PUBLIC_KEY:pub_test_tu_llave_publica}
wompi.private-key=${WOMPI_PRIVATE_KEY:prv_test_tu_llave_privada}
wompi.events-key=${WOMPI_EVENTS_KEY:test_events_tu_llave}
wompi.redirect-url=${WOMPI_REDIRECT_URL:http://localhost:4200/payment/result}